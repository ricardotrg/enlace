
# --- DataSource (compose) ---
spring.datasource.url=jdbc:postgresql://postgres:5432/gpstracker
spring.datasource.username=gps
spring.datasource.password=gps
spring.datasource.driver-class-name=org.postgresql.Driver

# --- Traccar (compose) ---
traccar.base-url=http://traccar:8082
traccar.user=ruiseart95@gmail.com
traccar.pass=ROMULOYrem1
traccar.device-id=4

# --- CORS (compose) ---
# Si entras por Caddy en http://localhost (puerto 80), usa esto:
cors.allowed-origins=http://localhost
# Si sigues entrando directo al Vite port 5175, usa en su lugar:
# cors.allowed-origins=http://localhost:5175
